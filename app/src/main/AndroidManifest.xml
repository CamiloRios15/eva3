<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.eva3aplicacionesmoviles">

    <!-- Permiso para las notificaciones en Android 13 y superior -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eva3AplicacionesMoviles"
        tools:targetApi="31">

        <!-- MainActivity: Pantalla principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AddItemActivity: Nueva actividad para agregar ítems -->
        <activity android:name=".AddItemActivity" />

        <!-- EditItemActivity: Nueva actividad para editar ítems -->
        <activity android:name=".EditItemActivity" />

        <!-- Agregar cualquier otra actividad si es necesario -->

    </application>

</manifest>


